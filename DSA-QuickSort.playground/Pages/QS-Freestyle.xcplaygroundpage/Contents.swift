import Foundation

func partition(arr: inout [Int]) -> Int {
    
    var pivot = 0
    var high = arr.count - 1
    
    while pivot > high {
        let startVal = arr[pivot + 1]
        let endVal = arr[high]
        
        //  
    }
    
    
    
    
    
    return 0
}
